# Frontend (Vite) env (BUILD-TIME)
#
# IMPORTANT:
# - Only variables prefixed with VITE_ are exposed to the browser.
# - Anything here is public (not secret). Do not put private keys or tokens here.
#
# Set this to your backend URL (cloudflared tunnel / domain). For local dev:
VITE_API_BASE_URL=http://localhost:3001
#
# Admin Telegram user id allowed to see the Settings button in the UI (not a secret).
VITE_ADMIN_TELEGRAM_ID=123456789
